# Formulario-Login-Registro-Deslizante
Un formulario dual (iniciar sesión y registro) con un panel animado que se desliza. Creado con HTML, CSS (usando transformy transition), y JavaScript (para manejar eventos del DOM). JS añade/quita una clase CSS para activar la animación gradual.
